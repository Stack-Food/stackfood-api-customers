apiVersion: apps/v1
kind: Deployment
metadata:
  name: stackfood-customers
  labels:
    app.kubernetes.io/name: stackfood-customers
    app.kubernetes.io/instance: stackfood-customers
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: stackfood
    app: stackfood-customers
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: stackfood-customers
      app: stackfood-customers
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app.kubernetes.io/name: stackfood-customers
        app.kubernetes.io/instance: stackfood-customers
        app.kubernetes.io/component: backend
        app.kubernetes.io/part-of: stackfood
        app: stackfood-customers
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/path: "/metrics"
        prometheus.io/port: "8084"
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: customers
          image: ghcr.io/stack-food/stackfood-api-customers:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8084
              name: http
              protocol: TCP
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          envFrom:
            - configMapRef:
                name: stackfood-customers-config
            - secretRef:
                name: stackfood-customers-secrets
          env:
            - name: ASPNETCORE_ENVIRONMENT
              value: "Production"
            - name: ASPNETCORE_URLS
              value: "http://+:8084"
          livenessProbe:
            httpGet:
              path: /health
              port: 8084
              scheme: HTTP
            initialDelaySeconds: 60
            periodSeconds: 15
            timeoutSeconds: 10
            failureThreshold: 5
          readinessProbe:
            httpGet:
              path: /health
              port: 8084
              scheme: HTTP
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
